spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/accounts
    
    username: postgres
    password: Afsar007!
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9090        